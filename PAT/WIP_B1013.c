#include <stdio.h>
#include <math.h>

int primeArr() {

    for (int i = 2; i < 10000; i++) {

    }
}


int main()
{
    int M,N,num;
    scanf("%d %d", &M, &N);
    for (int i = M; i < N; i++) {

    }

}

/**
 * 难点在于要输出10000个质数，并在其中挑选打印
 *  重点在每十个数字输出一行
 * */