#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int* selfDividingNumbers(int left, int right, int* returnSize){
    malloc(right - left);
    for (int i = left; i < right; i++) {
        i
    }
}

int main(){
    int* arr = {0};
    selfDividingNumbers();
    free()
}